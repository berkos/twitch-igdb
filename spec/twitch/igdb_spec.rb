# frozen_string_literal: true

RSpec.describe Twitch::Igdb do
  it "has a version number" do
    expect(Twitch::Igdb::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
